using System;
using System.Collections.Generic;

public class Dijkstra{

    /*public static void FindPath<NodeType>(
        IGraph<NodeType> graph,
        NodeType startNode, NodeType endNode,
        List<NodeType> outputPath, int maxiterations = 1000) {
        Boolean[] visited = new Boolean[maxiterations];
        float[] dist = new float[maxiterations];
        NodeType[] pred = new NodeType[maxiterations];

        for (int i = 0; i < maxiterations; i++) {
            visited[i] = false;
            dist[i] = 100000000f;
            //pred[i] = null
        }
        dist[startNode] = 0f;
        visited[startNode] = true;

        PriorityQueue<NodeType> openQueue = new PriorityQueue<NodeType>();
        openQueue.Enqueue(startNode);


    }*/



}